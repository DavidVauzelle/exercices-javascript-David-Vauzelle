// Créez un tableau contenant plusieurs couleurs. 
// Affichez la longueur du tableau dans la console, 
// puis ajoutez deux couleurs supplémentaires 
// et affichez à nouveau la longueur pour voir le changement.

let couleurs = ["bleu", "blanc", "rouge"];

console.log(couleurs.length);

// Ajout de deux couleurs
couleurs.push("jaune", "vert");

// Afficher à nouveau la longeur du tableau
console.log(couleurs.length);