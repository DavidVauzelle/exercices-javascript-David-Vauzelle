// Réalisez un compteur descendant de 5 à 0. 
// Pour chaque nombre, affichez le dans la console, et à la fin, affichez "Décollage".

let compteur = 5;

// Résultat attendu dans la console :

while (compteur > 0) {
    console.log(compteur);
    compteur--;
}
console.log("Décollage");
