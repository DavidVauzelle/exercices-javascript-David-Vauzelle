// Utilisez une boucle for pour afficher la table de multiplication par 7, de 1 à 10. 
// Pour chaque multiplication, affichez le résultat dans la console.

// Résultat attendu dans la console :
let table = 7;
for (let i = 1; i <= 10; i++) {
    console.log(`${table} x ${i} = ${7 * i}`);
}